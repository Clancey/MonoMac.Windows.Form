using System;
using MonoMac.Foundation;
using System.Collections.Generic;
namespace System.Windows.Forms
{
	public static class NSIndexSetExtender
	{
		/*
		public static uint[] ToArray(this NSIndexSet indexSet)
		{
			List<uint> indexes = new List<uint>();
			for (uint i = indexSet.FirstIndex; i <= indexSet.LastIndex;)
			{
				indexes.Add(i);
				i = indexSet.IndexGreaterThan(i);
			}
			return indexes.ToArray();
		}
		
		public static NSIndexSet FromArray(this NSIndexSet indexSet, uint[] indexes)
		{
			var indexSet = new NSMutableIndexSet();
			foreach(var index in indexes)
			{
				indexSet.Add(index);	
			}
			return indexSet;
			
		}
		*/
		
	}
}

